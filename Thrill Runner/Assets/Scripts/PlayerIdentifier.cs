using UnityEngine;

namespace ThrillRunner
{
    /// <summary>
    /// Marker component used to identify the Player without relying on tags.
    /// </summary>
    public class PlayerIdentifier : MonoBehaviour
    {
        // Empty on purpose - just used for GetComponent checks.
        //
        void Start() {
        }
    }
}
